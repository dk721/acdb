# Курсовой проект
Асинхронный веб-сервер на `C` с `postgresql`

Компилируется на `gentoo amd64 glibc openrc`, остальные дистрибутвы могут не скомпилировать сразу потребуется вмешательство.

---
Я прекрасно осведомлён о вещах что неправильно реализованы на сервере, в особенности о работе со строками и вероятными утечками памяти при переполнении буфера, но я просто не успел.
